{% extends 'base.html.twig' %}

{% block title %}Réservation Fleur De Lys{% endblock %}

{% block body %}
    <h1>Réservation Fleur De Lys</h1>

    <table class="table">
        <tbody>
           
            <tr>
                <th>Nom_user</th>
                <td>{{ reservationfleur.User }}</td>
            </tr>
            <tr>
                <th>End_date</th>
                <td>{{ reservationfleur.startDate ? reservationfleur.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ reservationfleur.endDate ? reservationfleur.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nbr_personne</th>
                <td>{{ reservationfleur.nbrPersonne }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reservationfleur.description }}</td>
            </tr>
        </tbody>
    </table>
    <div>
    <a href="{{ path('reservationfleur_index') }}"><button class="btn btn-light" >Voir La Liste</button></a> {{ include('reservationfleur/_delete_form.html.twig') }}
    </div>
     <style>
      h1{
         padding: 5%;
         font-family: 'Playfair Display', serif;
         color: rgb(252, 252, 250);
         font-size:35px;
         text-shadow: 3px 4px 0 rgba(14, 12, 12, 0.2); 
      }
    </style>

   
{% endblock %}
