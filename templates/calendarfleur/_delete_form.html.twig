<form method="post" action="{{ path('calendarfleur_delete', {'id': calendarfleur.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ calendarfleur.id) }}">
    <button class="btn">Delete</button>
</form>
