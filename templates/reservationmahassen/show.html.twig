{% extends 'base.html.twig' %}

{% block title %}Reservationmahassen{% endblock %}

{% block body %}
    <h1>Reservationmahassen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservationmahassen.id }}</td>
            </tr>
            <tr>
                <th>Nbr_personne</th>
                <td>{{ reservationmahassen.nbrPersonne }}</td>
            </tr>
            <tr>
                <th>Start_date</th>
                <td>{{ reservationmahassen.startDate ? reservationmahassen.startDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>End_date</th>
                <td>{{ reservationmahassen.endDate ? reservationmahassen.endDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ reservationmahassen.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservationmahassen_index') }}">back to list</a>

    <a href="{{ path('reservationmahassen_edit', {'id': reservationmahassen.id}) }}">edit</a>

    {{ include('reservationmahassen/_delete_form.html.twig') }}
{% endblock %}
