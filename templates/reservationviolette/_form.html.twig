{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-secondary">{{ button_label|default('Réserver') }}</button>
{{ form_end(form) }}
<style>
button{
    
    width:200px;
    font-size:10px;
    margin-bottom:5px;
}


</style>