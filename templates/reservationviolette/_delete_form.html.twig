<form method="post" action="{{ path('reservationviolette_delete', {'id': reservationviolette.id}) }}" onsubmit="return confirm('Êtes-vous sûr de bien vouloir supprimer cet élément?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservationviolette.id) }}">
    <button class="btn btn-secondary">Annuler</button>
</form>
<style>

button{
   
    width:200px;
    font-size:10px;
    margin-bottom:5px;
}

</style>